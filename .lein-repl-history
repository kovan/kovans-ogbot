(require 'ogbot.core)
(System/exit 0)
(require 'ogbot.core)
(println "SUCCESS")
(System/exit 0)
(require 'ogbot.core)
(require 'ogbot.webui)
(println "✓ All modules loaded successfully")
(System/exit 0)
(require '[ogbot.web-selenium :as ws])
  (require '[ogbot.config :as config])
  (def cfg (config/load-bot-configuration "files/config/config.ini"))
  (def adapter (ws/create-web-adapter cfg {} nil nil))
(require '[ogbot.web-selenium :as ws])
  (require '[ogbot.config :as config])
  (def cfg (config/load-bot-configuration "files/config/config.ini"))
  (def adapter (ws/create-web-adapter cfg {} nil nil))
(require '[ogbot.web-selenium :as ws])
(require '[ogbot.config :as config])
(println "Loading config...")
(def cfg (config/load-bot-configuration "files/config/config.ini"))
(println "Config loaded:" (select-keys cfg [:webpage :username]))
(println "Creating adapter (launching Chrome)...")
(def adapter (ws/create-web-adapter cfg {} nil nil))
(println "Done!")
(require '[ogbot.web-selenium :as ws])
  (require '[ogbot.config :as config])
  (def cfg (config/load-bot-configuration "files/config/config.ini"))
  (def adapter (ws/create-web-adapter cfg {} nil nil))
